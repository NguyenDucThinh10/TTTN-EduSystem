import SubmissionDetail from '../../../components/submissions/SubmissionDetail';

export default function SubmissionDetailPage({ submission }) {
  return <SubmissionDetail submission={submission} />;
}
