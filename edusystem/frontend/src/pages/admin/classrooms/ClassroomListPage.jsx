import DataTable from "../../../components/tables/DataTable"; import StatusBadge from "../../../components/common/StatusBadge"; import PageTemplate from "../../PageTemplate";
export default function ClassroomListPage() { return <PageTemplate title="Danh sách lớp học phần" description="Theo dõi các lớp đang mở."><DataTable data={[{ id: 1, name: "CS101-01", course: "Nhập môn lập trình", teacher: "Trần Thị B", status: "ACTIVE" }]} columns={[{ key: "name", label: "Lớp" }, { key: "course", label: "Học phần" }, { key: "teacher", label: "Giảng viên" }, { key: "status", label: "Trạng thái", render: (row) => <StatusBadge status={row.status} /> }]} /></PageTemplate>; }
